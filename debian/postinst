#!/bin/sh
set -e

case "$1" in
    configure)
        # Unit enable/start is handled by dh_installsystemd (#DEBHELPER#).
        if command -v gtk-update-icon-cache >/dev/null 2>&1; then
            gtk-update-icon-cache -f /usr/share/icons/hicolor >/dev/null 2>&1 || true
        fi
        ;;
esac

#DEBHELPER#
exit 0
